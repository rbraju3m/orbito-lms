<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnRisky="true"
         failOnWarning="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Tests run against real MySQL: the production engine. SQLite would
             hide collation, JSON, FK and index behaviour we depend on. -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="orbito_lms_test"/>
        <!-- Academies get their OWN prefix under test, and this is not
             cosmetic. `UsesSharedTenant::tearDownUsesSharedTenant()` drops
             every schema matching the prefix after every test, keeping only
             the shared one. Sharing the prefix with development therefore
             wiped the developer's own academies mid-run — a `db:seed` racing
             the suite failed with "Unknown database" partway through a tenant
             migration, because the suite had just dropped it. The prefix is
             the boundary the teardown scans, so the boundary has to differ. -->
        <env name="TENANCY_DB_PREFIX" value="orbito_lms_test_tenant_"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="FRONTEND_URL" value="http://localhost:5173"/>
    </php>
</phpunit>
